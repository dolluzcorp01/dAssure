{
    // F5 starts the dAssure API (server.js) on TPRM_PORT (4009) with the
    // debugger attached. The first entry is what F5 runs by default.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "dAssure: backend (server.js)",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/server.js",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal",
            "skipFiles": ["<node_internals>/**"]
            // No "restart": true here. With console=integratedTerminal, js-debug
            // ends the session as soon as it launches, and the server goes down
            // with it - "Waiting for the debugger to disconnect..." then exit.
        },
        {
            "name": "dAssure: frontend (react-scripts)",
            "type": "node",
            "request": "launch",
            "runtimeExecutable": "npm",
            "runtimeArgs": ["start"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "skipFiles": ["<node_internals>/**"]
        },
        {
            "name": "dAssure: run migrations",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/db/migrate.js",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal",
            "skipFiles": ["<node_internals>/**"]
        }
    ],
    "compounds": [
        {
            "name": "dAssure: full stack",
            "configurations": [
                "dAssure: backend (server.js)",
                "dAssure: frontend (react-scripts)"
            ]
        }
    ]
}
